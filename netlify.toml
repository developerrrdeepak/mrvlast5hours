[build]
  command = "npm run build:client"
  functions = "netlify/functions"
  publish = "dist/spa"
  environment = { SECRETS_SCAN_OMIT_PATHS = "*.md,.env.example,AGENTS.md,SETUP_GUIDE.md,PRODUCTION_READY.md,NETLIFY_*.md,AUTH_TESTING_GUIDE.md,.builder/**/*,scripts/**/*,docs/**/*,.netlify/plugins/**/*,pnpm-lock.yaml" }

[functions]
  node_bundler = "esbuild"

# API redirects
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true

# SPA fallback - serve index.html for all non-API routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
